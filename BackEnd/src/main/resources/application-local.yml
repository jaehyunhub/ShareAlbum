spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:h2:tcp://localhost/~/shareAlbum
    driver-class-name: org.h2.Driver
    username: sa
    password:

file:
  upload-dir: src/main/resources/static/images/
web:
  resources:
    static-locations: src/main/resources/static/images/
